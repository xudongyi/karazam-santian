$().ready(function() {    // $(".myAvatar").colorbox({inline:true,overlayClose: false, width:"400px", height:"250px"});	var $avatarForm = $("#avatarForm"), $submit = $avatarForm.find(":submit");	$submit.prop("disabled", false);	var $validate = $avatarForm.validate({		rules: {			avatarFile: "required"		},		messages: {			avatarFile: "请选择头像"		},		errorPlacement: function(error, element) {			element.closest("div").find(".form-remark").text(error.text());		},		unhighlight: function(element) {			$(element).closest("div").find(".form-remark").text("");		},		submitHandler: function(form) {			$submit.prop("disabled", true);			form.submit();		}	});});function PreviewImage(fileObj, imgPreviewId, divPreviewId) {    var browserVersion = window.navigator.userAgent.toUpperCase();    if (fileObj.files) {//兼容chrome、火狐7+、360浏览器5.5+等，应该也兼容ie10，HTML5实现预览        if (window.FileReader) {            var reader = new FileReader();            reader.onload = function(e) {                document.getElementById(imgPreviewId).setAttribute("src", e.target.result);                $("#divPreview").removeClass("hide");            }            reader.readAsDataURL(fileObj.files[0]);        } else if (browserVersion.indexOf("SAFARI") > -1) {            layer.msg("不支持Safari浏览器6.0以下版本的图片预览!");        } else {            layer.msg("不支持您当前使用的浏览器的图片预览!");        }    } else if (browserVersion.indexOf("MSIE") > -1) {//ie、360低版本预览        if (browserVersion.indexOf("MSIE 6") > -1) {//ie6            document.getElementById(imgPreviewId).setAttribute("src", fileObj.value);            $("#divPreview").removeClass("hide");        } else {//ie[7-9]            fileObj.select();            if (browserVersion.indexOf("MSIE 9") > -1) {                document.getElementById(divPreviewId).focus();                $("#divPreview").removeClass("hide");            }            var newPreview = document.getElementById(divPreviewId + "New");            if (newPreview == null) {                newPreview = document.createElement("div");                newPreview.setAttribute("id", divPreviewId + "New");                newPreview.style.width = document.getElementById(imgPreviewId).width + "px";                newPreview.style.height = document.getElementById(imgPreviewId).height + "px";                newPreview.style.border = "solid 1px #d2e2e2";                $("#divPreview").removeClass("hide");            }            newPreview.style.filter = "progid:DXImageTransform.Microsoft.AlphaImageLoader(sizingMethod='scale',src='" + document.selection.createRange().text + "')";            var tempDivPreview = document.getElementById(divPreviewId);            tempDivPreview.parentNode.insertBefore(newPreview, tempDivPreview);            tempDivPreview.style.display = "none";            $("#divPreview").removeClass("hide");        }    } else if (browserVersion.indexOf("FIREFOX") > -1) {//firefox        var firefoxVersion = parseFloat(browserVersion.toLowerCase().match(/firefox\/([\d.]+)/)[1]);        if (firefoxVersion < 7) {//firefox7以下版本            document.getElementById(imgPreviewId).setAttribute("src", fileObj.files[0].getAsDataURL());            $("#divPreview").removeClass("hide");        } else {//firefox7.0+            document.getElementById(imgPreviewId).setAttribute("src", window.URL.createObjectURL(fileObj.files[0]));            $("#divPreview").removeClass("hide");        }    } else {        layer.msg("不支持您当前使用的浏览器的图片预览!");    }}